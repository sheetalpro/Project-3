/* Basic Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  line-height: 1.6;
}

header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px;
  background: #333;
  color: #fff;
}

header .logo {
  font-size: 24px;
}

header nav ul {
  display: flex;
  list-style: none;
}

header nav ul li {
  margin-left: 20px;
}

header nav ul li a {
  color: #fff;
  text-decoration: none;
}

.hero {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  padding: 50px;
  background: #f4f4f4;
}

.hero-text {
  flex: 1;
}

.hero-image {
  flex: 1;
}

.hero-image img {
  width: 100%;
  max-width: 400px;
}

.btn {
  display: inline-block;
  padding: 10px 20px;
  background: #333;
  color: #fff;
  text-decoration: none;
  margin-top: 20px;
}

.features, .pricing, .testimonials, .newsletter {
  padding: 50px 20px;
  text-align: center;
}

.feature-list, .plans, .testimonial-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

.feature, .plan, .testimonial {
  flex: 1 1 250px;
  border: 1px solid #ddd;
  padding: 20px;
}

.newsletter form {
  display: flex;
  justify-content: center;
}

.newsletter input {
  padding: 10px;
  width: 250px;
}

.newsletter button {
  padding: 10px;
  background: #333;
  color: #fff;
  border: none;
}

footer {
  text-align: center;
  padding: 20px;
  background: #333;
  color: #fff;
}

/* Responsive */
@media (max-width: 768px) {
  .hero {
    flex-direction: column;
    text-align: center;
  }

  header nav ul {
    flex-direction: column;
  }

  header nav ul li {
    margin: 10px 0;
  }
}